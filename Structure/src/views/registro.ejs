<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <link href="/css/registroStyles.css" rel="stylesheet" type="text/css">
    <title>Registro</title>
</head>
<body>
    <!-- Header -->
    <%- include('./partials/header') %>
  
    <!-- Body -->
    <main>
        <div class="contenedor">
            <form action="/users/register" method="post">
                <h3>Crea tu cuenta</h3>
                <div class="full_name">
                    <div> 
                        <label for="firstName">Nombre</label>
                        <input type="text" name="firstName" id="firstName" required value="<%=locals.usuario && usuario.firstName ? usuario.firstName : null%>">
                        <% if (locals.errors && errors.firstName) {%>
                            <p class="feedback"><%=errors.firstName.msg%></p>
                        <% } %>
                    </div>
                    <div>
                        <label for="lastName">Apellido</label>
                        <input type="text" name="lastName" id="lastName" required value="<%=locals.usuario && usuario.lastName ? usuario.lastName : null%>">
                        <% if (locals.errors && errors.lastName) {%>
                            <p class="feedback">Error<%=errors.lastName.msg%></p>
                        <% } %>
                    </div>
                </div>
                <div class="contenedor_email">
                    <label for="email">Correo electronico</label>
                    <input type="email" name="email" id="email" required value="<%=locals.usuario && usuario.email ? usuario.email : null%>">
                    <% if (locals.errors && errors.email) {%>
                        <p class="feedback"><%=errors.email.msg%></p>
                    <% } %>
                </div>
                <div class="contenedor_password">
                    <label for="password">Contraseña</label>
                    <input type="password" name="password" id="password" required value="<%=locals.usuario && usuario.password ? usuario.password : null%>">
                    <% if (locals.errors && errors.password) {%>
                        <p class="feedback"><%=errors.password.msg%></p>
                    <% } %>
                </div>
                <div class="contenedor_button">
                    <button type="submit">Registrarse</button>
                    <p>¿Ya te registraste? <a href="/users/login">Inicia Sesión</a></p>
                    <p>Al registrarte, aceptas los <a href="">Términos y Condiciones</a> y la 
                        <a href="">Política de privacidad</a> de RAStyle</p>
                </div>
            </form>
        </div>

    </main>

    <!-- Footer -->
    <%- include('./partials/footer') %>

</body>
</html>